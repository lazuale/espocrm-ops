{
  "docker_log": [
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps -q db",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE up -d db",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps -q db",
    "inspect --format {{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}} mock-db",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps --status running --services",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE stop espocrm espocrm-daemon espocrm-websocket",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps -q db",
    "version --format {{.Server.Version}}",
    "inspect --format {{.State.Running}} mock-db",
    "exec mock-db mariadb --version",
    "exec -i -e MYSQL_PWD mock-db mariadb -u root",
    "exec -i -e MYSQL_PWD mock-db mariadb -u root espocrm",
    "image inspect alpine:3.20",
    "run --pull=never --rm --user 0:0 --entrypoint sh -v REPLACE_STORAGE_DIR:/espo-storage -e ESPO_RUNTIME_UID -e ESPO_RUNTIME_GID alpine:3.20 -euc",
    "storage=\"/espo-storage\"",
    "chown -R \"$ESPO_RUNTIME_UID:$ESPO_RUNTIME_GID\" \"$storage\"",
    "find \"$storage\" -type d -exec chmod 0755 {} +",
    "for relative in data custom client/custom upload; do",
    "path=\"$storage/$relative\"",
    "if [ -d \"$path\" ]; then",
    "find \"$path\" -type d -exec chmod 0775 {} +",
    "find \"$path\" -type f -exec chmod 0664 {} +",
    "fi",
    "done",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE up -d",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps -q db",
    "inspect --format {{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}} mock-db",
    "compose --project-directory REPLACE_PROJECT_DIR -f REPLACE_COMPOSE_FILE --env-file REPLACE_TARGET_ENV_FILE ps -q espocrm",
    "inspect --format {{if .State.Health}}{{.State.Health.Status}}{{else}}{{.State.Status}}{{end}} mock-espocrm",
    "inspect --format {{if .State.Health}}{{range .State.Health.Log}}{{.Output}}{{printf \"\\n\"}}{{end}}{{end}} mock-espocrm"
  ],
  "process_exit_code": 5,
  "running_services_after": [
    "db",
    "espocrm",
    "espocrm-daemon",
    "espocrm-websocket"
  ],
  "running_services_before": null
}
